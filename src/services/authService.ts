// Service gọi API liên quan đến xác thực (Authentication).
// Các hàm: login(), register(), logout(), refreshToken().
// Sử dụng axios instance từ ./api.ts để gọi đến backend endpoint /auth/*.
// Được gọi bởi: authSlice (trong các async thunks).
