// Redux slice quản lý trạng thái xác thực (Authentication).
// Lưu trữ: thông tin user đang đăng nhập, token JWT, trạng thái loading/error.
// Actions: login, register, logout, loadUserFromToken.
// Được sử dụng bởi: LoginPage, RegisterPage, PrivateRoute, Header (hiển thị avatar).
