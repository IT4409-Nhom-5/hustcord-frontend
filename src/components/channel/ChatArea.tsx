// Khu vực chat chính - chiếm phần lớn màn hình bên phải.
// Bao gồm: ChannelHeader (trên cùng), MessageList (giữa), MessageBox (dưới cùng).
// Hiển thị nội dung của channel đang được chọn.
// Tương ứng với "text-based-channel.tsx" trong code gốc.
