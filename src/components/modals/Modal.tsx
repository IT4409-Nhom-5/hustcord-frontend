// Modal base component - khung modal dùng chung cho toàn bộ app.
// Cung cấp: overlay tối, animation mở/đóng, nút close, click outside để đóng.
// Các modal cụ thể sẽ import và sử dụng component này làm wrapper.
// Tương ứng với "modal.tsx" trong code gốc.
